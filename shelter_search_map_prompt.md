# 避難所検索・地図機能を実装するためのプロンプト

以下の要件を満たす避難所検索画面と地図機能を、既存システムへ追加実装してください。

## 基本方針

- まず既存のプロジェクト構成、使用技術、ルーティング、テンプレート、避難所データ構造を確認してください。
- 既存の画面デザイン、検索機能、データ構造、API、認証などをできるだけ維持してください。
- 今回必要な変更だけを行い、無関係なファイルや機能は変更しないでください。
- 既存の避難所データがある場合は、それを利用してください。
- 存在しない項目が必要な場合は、既存構造を確認したうえで必要最小限の項目だけ追加してください。
- 実装後は、構文チェック、検索処理、画面表示、地図連動を実際に検証してください。
- エラーやアクセスブロックが発生しないよう、既存の起動方法・デプロイ方法に合わせてください。

## 1. 避難所検索画面

避難所検索画面を、次の構成にしてください。

- 左側: 検索条件フォーム
- 右側: 地図
- スマートフォンなど画面幅が狭い場合は、検索フォームと地図を縦方向に並べるレスポンシブ表示
- 既存の画面デザインや色、ボタン、チェックボックスの雰囲気をできるだけ維持

## 2. 地図

地図は Leaflet など、既存システムに適した地図ライブラリを使用してください。

- 日本全体を初期表示
- 国土地理院の標準地図タイルを使用
- タイル URL:
  `https://cyberjapandata.gsi.go.jp/xyz/std/{z}/{x}/{y}.png`
- 必要に応じて国土地理院の淡色地図を切替表示できるようにする
- 地図上に国土地理院の出典表示を付ける
- マウスドラッグで移動できる
- マウスホイールで拡大・縮小できる
- `+` / `-` ボタンで拡大・縮小できる
- 地図移動後、表示範囲内の避難所を更新する
- 地図の拡大縮小後、表示対象の避難所を更新する
- 広域表示時は、すべての避難所を細かく表示しすぎない
- 広域では近隣の避難所をまとめる、または代表表示する
- 地域表示・詳細表示では、個別の避難所を表示する
- 青森市周辺まで拡大した場合、青森市内の避難所を確認できる
- さらに拡大した場合、地域の避難所をより詳しく確認できる

### 避難所ピン

- 避難所は赤色のピンで表示する
- 地図の表示範囲内にある避難所だけを表示する
- 検索条件に一致しない避難所は地図に表示しない
- 避難所ピンをクリックすると、ポップアップで基本情報を表示する
- ポップアップには少なくとも以下を表示する
  - 避難所名
  - エリア・地区
  - 収容人数
  - ペット可否
  - バリアフリー
  - 駐車場
  - 高齢者・障がいのある方への配慮
  - 対応災害

### 現在地

検索フォームに「位置情報を許可して現在地を取得」ボタンを追加してください。

- ブラウザの Geolocation API を使用する
- 位置情報の許可を求める
- 現在地は黒色のピンで表示する
- 現在地取得後、地図を現在地付近へ移動する
- 現在地取得後、周辺の避難所を確認しやすい縮尺まで拡大する
- 現在地取得に失敗した場合は、ユーザーに分かりやすいエラーを表示する
- 位置情報が使えないブラウザにも対応する
- 位置情報取得中、取得成功、権限拒否、タイムアウト、取得失敗の状態を画面上に表示する
- Permissions API が利用できる場合は、位置情報の許可状態を画面上に表示する
- HTTPS でない外部 URL では位置情報を取得できないため、その理由と `localhost` または HTTPS URL を使用する案内を表示する
- ブラウザのサイト設定で位置情報が拒否されている場合は、許可に変更する案内を表示する
- 位置情報取得処理が地図ライブラリの読み込み失敗に巻き込まれて停止しない構成にする

## 3. フリー検索

位置情報と避難所条件の間に、フリー検索欄を追加してください。

検索対象は、避難所データに含まれている情報全体としてください。例えば以下です。

- 避難所名
- エリア・地区
- 設備情報
- 対応災害
- 収容人数
- ペット可否
- バリアフリー情報
- 駐車場情報
- 高齢者・障がいのある方への配慮情報

複数のキーワードを入力できるようにしてください。

次の文字をキーワードの区切りとして扱ってください。

- 半角スペース
- 全角スペース
- 半角カンマ `,`
- 読点 `、`
- 句点 `。`

以下はすべて、2つのキーワードとして扱ってください。

- `青森駅 駐車場`
- `青森駅　駐車場`
- `青森駅, 駐車場`
- `青森駅、駐車場`
- `青森駅。駐車場`

複数キーワードの場合は、入力されたすべてのキーワードに一致する避難所だけを表示してください。

## 4. 避難所の検索条件

既存の条件を残したまま、以下の条件を追加してください。

### 既存条件

チェックボックスで選択できるようにしてください。

- ペット可能
- バリアフリーあり
- 未就学児がいる
- 津波
- 洪水
- 地滑り
- 土石流

### 追加条件

#### 避難したい人数

- 数値を入力できる項目を追加する
- 入力された人数以上の収容人数がある避難所だけを表示する
- 未入力の場合は人数条件を適用しない
- 数値以外や不正な値でサーバーエラーを起こさない

#### 駐車場あり

- 「駐車場あり」チェックボックスを追加する
- チェック時は駐車場が利用可能な避難所だけを表示する

#### 高齢者・障がいのある方への配慮あり

- 「高齢者・障がいのある方への配慮あり」チェックボックスを追加する
- チェック時は該当する避難所だけを表示する

## 5. 検索処理の組み合わせ

すべての検索条件は組み合わせて使用できるようにしてください。

- フリー検索
- 避難したい人数
- ペット可能
- バリアフリーあり
- 未就学児がいる
- 駐車場あり
- 高齢者・障がいのある方への配慮あり
- 災害の種類
- 既存システムにあるその他の検索条件

複数の条件を指定した場合は、すべての条件を満たす避難所だけを検索結果に表示してください。

検索条件の適用先は次の両方です。

- 検索結果の避難所一覧
- 地図上の赤い避難所ピン

つまり、検索条件に一致しない避難所は、一覧と地図の両方から除外してください。

## 6. データ構造

既存の避難所データを確認し、すでに存在する項目はそのまま利用してください。

不足している場合のみ、必要最小限として次の項目を追加してください。

```json
{
  "id": 1,
  "name": "避難所名",
  "latitude": 40.8246,
  "longitude": 140.7400,
  "district": "中央",
  "capacity": 300,
  "pet_friendly": true,
  "barrier_free": true,
  "has_preschool_children": true,
  "parking_available": true,
  "senior_disability_support": true,
  "disaster_types": ["津波", "洪水"]
}
```

注意事項:

- 実際に存在しない大量の避難所データを勝手に作成しない
- 既存データが座標を持たない場合は、地図表示に必要な座標だけを追加する
- 既存データに収容人数などがない場合は、既存データ件数に合わせて必要最小限の項目を追加する
- 登録機能が存在する場合は、既存の登録処理を壊さない
- 古い避難所データに新項目がない場合も、エラーにならないようデフォルト値を設定する

## 7. 実装上の注意

- サーバー側で検索条件を適用し、検索結果一覧に反映する
- ブラウザ側でも同じ検索条件を適用し、地図上のピンに反映する
- サーバー側とブラウザ側で条件の判定が食い違わないようにする
- 検索条件を変更したときは、地図上のピンを即時更新する
- 地図の `moveend` と `zoomend` などを利用し、表示範囲変更時にピンを更新する
- ユーザー入力をポップアップに表示する場合は、HTML として直接解釈されないようエスケープする
- 外部地図タイルを使用する場合は、正しい出典表示を行う
- 既存の API がある場合は、必要に応じて同じ検索条件を API にも反映する

## 8. 完了条件

次のすべてを満たした状態で完了してください。

- 検索画面の左側に既存フォームが表示される
- 検索画面の右側に詳細な日本の道路地図が表示される
- 市名・地名・道路名が見やすい
- 地図の拡大・縮小・移動ができる
- 避難所が赤いピンで表示される
- 現在地が黒いピンで表示される
- 避難所ピンをクリックすると基本情報が表示される
- フリー検索が使える
- 複数キーワードと指定された区切り文字が使える
- 避難したい人数で収容人数を絞り込める
- 駐車場ありで絞り込める
- 高齢者・障がいのある方への配慮ありで絞り込める
- 既存のチェックボックス条件が動作する
- フリー検索とすべての条件を組み合わせられる
- 検索結果一覧と地図上のピンが同じ条件で更新される
- 地図の表示範囲と縮尺に応じてピン表示が更新される
- 既存の画面・機能が壊れていない
- 構文チェックと主要な検索パターンの実動作確認が完了している

実装後は、変更したファイル、追加したデータ項目、実行した検証内容、残っている注意点を簡潔に報告してください。
